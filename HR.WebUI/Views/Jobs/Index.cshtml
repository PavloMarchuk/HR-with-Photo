@model IEnumerable<HR.DateLayer.DbLayer.JobTitle>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NameJobTitle)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameJobTitle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JobTitleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JobTitleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JobTitleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
